import { Button } from '@mui/material';
import { useEffect } from 'react';

const Test = () => {
  useEffect(() => {
    console.log('hello');
  });
  return (
    <Button variant='contained' color='warning'>
      T$DSADADASDAD COSAS
    </Button>
  );
};

export default Test;
